# syntax=docker/dockerfile:1
FROM nginx:1.25-alpine

# Static assets could be built and copied here if a separate SPA exists.
# For now, FastAPI serves the root page; this container is optional.

COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
